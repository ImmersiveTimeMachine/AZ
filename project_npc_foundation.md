---
name: project_npc_foundation
description: "LIVE resume point for the Chalkie infected NPC build on branch feature/NPC. Holds the SHIPPED v2 AI pawn foundation (committed 496920c), the locked NPC ARCHITECTURE decision (Option B: Mover movement + classic ABP, NOT Motion Matching), the chosen mesh+anims (SK_ZombieAC_A + Zombie_01 clips — SAME skeleton, NO retarget), the verified Mover<->navigation finding (UNavMoverComponent), and the exact pending steps. Read FIRST to resume NPC/AI/Chalkie work."
metadata: 
  node_type: memory
  type: project
  originSessionId: 5bce0c20-5866-4582-9e79-760a09865698
---

# Chalkie Infected NPC — foundation (resume point)

Branch **`feature/NPC`** (from `dev`, from `main`; `dev` + `feature/NPC` both pushed). First NPC = the **Chalkie infected** enemy ([[project_lore_chalk]]: rotting drug-addict, primal/aggressive, pure-threat; Phase-1 frozen-statue vs Phase-2 chase later). It rides the **v2 Mover MOVEMENT rails** (the [[project_v2_architecture]] AI-parity payoff) but does NOT share the hero's animation pipeline — see the architecture decision below. NOT on the deprecated v1 `AAZ_CharacterBase`/`AAZ_EnemyCharacter` (classic ACharacter+CMC).

## ★ NPC ARCHITECTURE DECISION — Option B "Hybrid" (locked 2026-06-26)
The real question is TWO axes, not one binary: **movement** (Mover vs CMC) and **animation** (Motion Matching+CHT vs classic ABP). The hero uses premium on both; NPCs don't have to.

**CHOSEN: B = Mover (movement) + classic ABP (animation). NOT Mover+CHT+MM (option A), NOT classic CMC (option C).**

- **Movement = Mover** (shared with hero). Keeps the committed `AAZ_PawnMoverInfectedCharacter`, the AI-parity `ProduceInput` intent surface, nav via `UNavMoverComponent`, and the clearance/traversal reuse — ONE movement codebase, lowest churn.
- **Animation = classic ABP** (state machine + blendspace, speed/state-driven). **NO Motion Matching, NO CHT/PoseSearch for NPCs.** MM runs a per-instance per-frame nearest-neighbour search (Epic's MM sample is a SINGLE-character showcase; it does not throttle/crowd). Classic ABP is a cheap SM eval AND unlocks the engine's crowd levers: URO (Update Rate Optimization), `VisibilityBasedAnimTickOption`, LOD tick-throttle, master-pose sharing. A shambling zombie gains ~nothing visually from MM.
- **DOCTRINE (reusable):** *movement system = shared (Mover); animation system = per-archetype.* Hero → MM; Chalkie/hordes → classic ABP.
- **Consequence:** last session's MM copy **`UAZ_InfectedAnimInstance` is REWRITTEN IN PLACE** (NOT deleted/recreated) — strip the MM ~70% (PoseSearch/Chooser/Trajectory/BlendStack), keep the Mover-cached refs + Native overrides. Name KEPT (consistent with `AAZ_PawnMoverInfectedCharacter`/`AAZ_InfectedAIController`; it's the generic infected driver). See "## Anim instance — LOCKED" below. The pawn's `UMoverTrajectoryPredictor` member is now unused too (only MM needed it) → **delete it from `AAZ_PawnMoverInfectedCharacter`** in the same pass.
- **ESCAPE HATCH → Option C (CMC + classic ABP):** only if enemy density becomes TRUE swarms (dozens on screen, L4D-style). CMC is the most proven/lightest crowd path (mature NavMesh + Detour/RVO avoidance, cheap simulated-proxy replication) but costs TWO movement systems + abandons the Mover pawn. Mover is still **Experimental** in 5.8 (fine for one hero, riskier for a whole enemy population at high N). The AI controller + intent design port to CMC later, so B is reversible. **Enemy-density target still TBD** (user hasn't answered sparse-dread vs swarm) → default stays B until profiling says otherwise.

## Mesh + anim packs (mesh REVISED 2026-06-27 — NO RETARGET NEEDED)
- **★ Mesh = `/Game/ZombiePackV1/ZombieAC/Mesh/ZombieAC_A/SK_ZombieAC_A`** — user-designated "the GOOD NPC to use with the good anims" (2026-06-27). Supersedes the earlier `SK_ZombieAA` pick. ZombieAC pack also has `SK_ZombieAC_B`; each body ships a OneMesh BP and a Parts/dismemberment BP (`BP_ZombieAC_A_OneMesh` / `BP_Zombie_AC_A_Parts`), plus per-part `BodyParts/SK_*` skeletal meshes (Arm/ForeArm/Hand/Leg/Knee/Head/Body) for dismemberment + a `SK_ZombieAC_A_PhysicsAsset`.
- **★ KEY FACT (verified on-disk via uasset import table, 2026-06-27): `SK_ZombieAC_A` is bound to skeleton `/Game/Zombie_01/Core/Character/Mesh/UE4_Mannequin_Skeleton` — the SAME skeleton the Zombie_01 anim clips use.** (The old `SK_ZombieAA` was on the pack's own `SNP_UE4_Mannequin_Skeleton`, a DIFFERENT asset; that's the only reason a retarget was ever on the table.) Because mesh + clips now share one skeleton asset, **the Zombie_01 anims apply DIRECTLY to `SK_ZombieAC_A` — NO RETARGETING, no Compatible-Skeletons setup.** `SNP_UE4_Mannequin_Skeleton` is no longer used by this NPC path.
- **Anims = `/Game/Zombie_01`** — rich classic clip set: idle / walk_F (many) / chase (run) / turn L/R 90+180 / attack / death / crawl / shamble / onfire / hyper; each in **Root** (`Animations/Root/`, root-motion) and **In-Place** (`Animations/InPlace/`, `_IPC` suffix) variants. Ships its own `ABP_DemoPlayable` + `BP_ZombiePlayable`. Skeleton: `/Game/Zombie_01/Core/Character/Mesh/UE4_Mannequin_Skeleton`.

## SHIPPED — commit 496920c (C++ only; CLI build Result: Succeeded; NO editor/BP yet)
Three new classes (hero code 100% untouched). All still VALID under Option B (only the anim choice changed):
- **`AAZ_PawnMoverInfectedCharacter`** (`Character/`) — STANDALONE `APawn` sibling of the hero (NOT a subclass). v2 Mover stack: `UAZ_PawnMoverComponent` (modes self-register in its `OnRegister` — no BP wiring), `UNetworkPredictionComponent`, `UMoverTrajectoryPredictor` (now unused under B), `UAZ_MovementDirectionCapabilityComponent` (clearance clamp, hero-agnostic, reused), and its OWN `UAZ_AbilitySystemComponent` (NPCs have no PlayerState; `SetReplicationMode(Minimal)`, InitAbilityActorInfo(this,this) in BeginPlay+PossessedBy). `DefaultTeamId=1`. NO camera / Enhanced Input. `PrimaryActorTick=false`. Implements IAbilitySystemInterface / IMoverInputProducerInterface / IGameplayTagAssetInterface / IGenericTeamAgentInterface. `AIControllerClass`+`AutoPossessAI=PlacedInWorldOrSpawned`.
  - **AI intent surface** (server-written by controller/BT, read by ProduceInput): `SetMoveIntentWorld` / `SetDesiredFacingWorld` / `SetGait` → cached fields. `ProduceInput` fills `FCharacterDefaultInputs` (DirectionalIntent move, `OrientationIntent`=facing, ControlRotation=facing, no jump) + `FAZ_MoverCustomInputs` (Gait, RotationMode=OrientToMovement, no crouch). **Verified fact:** walking mode facing honours `OrientationIntent` (its `ResolveRotationTarget` reads it; `RotationMode` only sets turn SPEED) — AI faces its target with zero mode changes; gait→speed via `Gait` (Walk165/Run375/Sprint585).
- **`AAZ_InfectedAIController`** (`AI/`) — possesses the pawn; **TEMP "home toward the player" drive** (`bDebugHomeToPlayer`, GetPlayerPawn(0), StopDistance=150) writing the intent surface + `SetControlRotation`. SCAFFOLDING to verify Mover+anim in PIE; replaced later by perception+BT+nav.
- **`UAZ_InfectedAnimInstance`** (`Animation/`) — currently an INDEPENDENT verbatim COPY of the hero's MM `UAZ_MoverAnimInstance`. **TO BE GUTTED IN PLACE** (Option-B; strip the ~70% PoseSearch/Chooser/Trajectory/BlendStack the classic Chalkie never uses) → becomes the slim classic Mover-read driver; name kept (see "## Anim instance — LOCKED").

## Navigation WITH Mover — VERIFIED (UE 5.8 engine, 2026-06-25)
YES. Engine ships **`UNavMoverComponent`** (`Mover/.../DefaultMovementSet/NavMoverComponent.h`) + optional `NavWalkingMode`/`AsyncNavWalkingMode`. It implements `INavMovementInterface`; the AIController's PathFollowingComponent **auto-discovers** it via `FindComponentByInterface<INavMovementInterface>()` (`PathFollowingComponent.cpp:1514`). PathFollowing calls `RequestDirectMove`/`RequestPathMove` → it CACHES the move; the pawn's `ProduceInput` pulls it via `ConsumeNavMovementData(OutIntent,OutVelocity)` and feeds the Mover cmd. Supports Detour Crowd avoidance (good for hordes). So: add `UNavMoverComponent` to the pawn, consume in ProduceInput (fall back to cached AI intent when no nav move), drop a `NavMeshBoundsVolume`, then standard BT `MoveTo`/`MoveToActor` works. `AIModule` already a dep.

## DONE THIS SESSION (2026-06-27) — anim instance gutted + asset pass + milestone 2 wired
- **C++ pass (committed `c7d6a39`)** — `UAZ_InfectedAnimInstance` GUTTED IN PLACE to the slim classic Mover driver (per "## Anim instance — LOCKED"): exposes `GroundSpeed` (=`Cached_MoverComponent->GetVelocity().Size2D()`), `bIsMoving` (>`MoveSpeedThreshold`=5), caches pawn+Mover in `NativeInitializeAnimation`, re-resolves lazily in `NativeUpdateAnimation`. `UMoverTrajectoryPredictor` member + include + ctor/BeginPlay wiring DELETED from `AAZ_PawnMoverInfectedCharacter` (MM-only artifact). CLI build clean.
- **Asset pass (uncommitted uassets)** — `AZ_ABP_Chalkie` (`/Game/AZ/Blueprints/Animation/`) DUPLICATED from `ABP_DemoPlayable`, reparented to `UAZ_InfectedAnimInstance`, compiles clean. Inherited pose graph kept (Locomotion SM `IdleWalkChase`=BlendSpacePlayer on `BS_IdleWalkChase` → BlendListByBool(Turning)→Turns SM; LayeredBoneBlend(AttackAlphaBlend)→Attacking SM). `BP_AZ_Chalkie` (`/Game/AZ/Blueprints/Character/Infected/`) = child of `AAZ_PawnMoverInfectedCharacter`; Mesh→`SK_ZombieAC_A`, AnimClass→`AZ_ABP_Chalkie_C`. **Smoke test PASSED — Chalkie slides toward player in idle** (full chain confirmed live).
- **★ Milestone 2 (speed-matched blendspace) — DONE, PIE-verify pending.** Measured each loco clip's TRUE root-motion ground speed (displacement/length) via an `unreal_execute_script` **cpp** static-initializer probe calling 5.8 `UAnimSequence::ExtractRootMotionFromRange(double Start, double End, const FAnimExtractContext&)` — note 5.8 SIG CHANGE: old `ExtractRootMotionFromRange(2 floats)` and `ExtractRootMotion(start,delta,bool)` are GONE; both now take an `FAnimExtractContext(CurrentTime, bExtractRootMotion=true)`. Results: **Walk_F_1_Full_Loop RAMPS 13→26→40 (~30 rep, not a clean loop), Chase_1_Full_Loop steady ~58, HyperChase_1 rock-solid 273 cm/s** — the zombie clips are SLOW (Chase < hero walk 165; only HyperChase out-paces a walking player). Reworked `BS_IdleWalkChase`: X-axis → **"Speed", real cm/s, min 0 max 600**, samples placed at their TRUE speeds **0 / 30 / 58 / 273**, **`axisToScaleAnimation=BSA_X`** (blendspace scales clip playback to the requested axis speed → slide-free at ANY Mover speed, incl. extrapolated beyond the top sample). `AZ_ABP_Chalkie:EventGraph` now feeds **raw `GroundSpeed`** (no divisor; via `write_graph_dsl`, compiled+saved). **DESIGN CHOICE (user 2026-06-27): "faster gameplay + scale anim"** — keep fast movement, let axisToScaleAnimation speed the clip up (homing Run=375 → HyperChase scaled ~1.37×, frantic chase, feet still planted). The alt ("slow shambler" = gait speeds set to clip speeds) was rejected. BS_IdleWalkChase edited **IN PLACE (pack content)** for fast iteration → **CLEANUP: duplicate into /Game/AZ/Blueprints/Animation/ + repoint (anim domain `set_blend_space`) before locking** (this axis change ALSO breaks the pack's own ABP_DemoPlayable which fed 0..3 — we don't ship it). 2 demo AnimNotify events preserved (harmless). **NEXT: PIE — watch foot slide at homing speed; then optionally bump homing gait for a player-out-sprinting Chalkie + commit (asset pass + BS + ABP all uncommitted).**

## PENDING — do next (under Option B)
4. Replace homing with **AIPerception (sight/hearing) + BehaviorTree + NavMesh path-follow via `UNavMoverComponent`** + `NavMeshBoundsVolume`. Team attitude solver. → **★ full phased plan: [[project_zombie_ai_plan]]** (drafted 2026-06-27, "first-class zombie reaction"; THIS is the next-session build — read it first).
5. Attacks/death/crawl (parameterized GA, ties to [[project_combat_fist_build_plan]]); health/attributes on the NPC ASC; Phase-1 frozen vs Phase-2 aggressive states. → folded into [[project_zombie_ai_plan]] Phases 3-4.
6. (Optional polish) cosmetic turn-in-place (drive `Turning`/`TurnType` from C++ yaw-delta into the dormant Turns SM), foot contact, idle variation.

## Root motion for the Chalkie — DECIDED 2026-06-27 (see [[reference_mover_root_motion]])
Mover RM is now fully mapped. The split for the Chalkie:
- **One-shots (attacks / lunge / stagger / knockdown / getup / death / crawl-start) = MONTAGE root motion** via `FLayeredMove_AnimRootMotion` — easiest through the **"Play Montage (Mover Actor)"** node / `UPlayMoverMontageCallbackProxy`. Self-ending, replicated (sim-proxy play-rate nudge), enforces one RM montage at a time. The Zombie_01 **Root** clips (authored RM) are perfect source for these. This is the clean primitive the whole game shares (zombie one-shots AND hero combat/traversal montages).
- **Travel (continuous nav locomotion) = intent + Mover, in-place anims** (Zombie_01 `_IPC` clips). NOT root motion — RM-driven travel can't be anim-tick-throttled, which kills the Option-B crowd levers. Doctrine (from the jump system): *RM owns phases terrain can't surprise (authored one-shots); intent+Mover owns phases it can (travel).*
- **Warping** (e.g. snap an attack lunge onto the player) IS available to us on the GT — add a `UMotionWarpingComponent` to the pawn + MotionWarping notify windows on the montage; Mover auto-adapts. NOT the ChaosMover async path (we're on NetworkPrediction).
- **Backend stays NetworkPrediction.** ChaosMover async would only win if the Chalkie must be a physics citizen (ragdoll/physics hit-reactions, destruction) — flagged as the most likely future trigger, not now. Full decision guide in [[reference_mover_root_motion]].

## Anim instance — LOCKED 2026-06-27 (modify in place, do NOT recreate)
**REWRITE `UAZ_InfectedAnimInstance` IN PLACE** — strip the MM ~70% (PoseSearch/Chooser/Trajectory/BlendStack), keep the Mover-cached refs + Native overrides + BeginPlay caching. **Do NOT delete+recreate**: nothing references the class yet (gutting is safe), the name stays consistent with `AAZ_PawnMoverInfectedCharacter`/`AAZ_InfectedAIController`, and it reads as the GENERIC infected locomotion driver (reusable for future infected variants; `AZ_ABP_Chalkie` is the specific creature ASSET that uses it). No build-cost difference (UPROPERTY removals need a full rebuild either way), less churn than a rename. Anim instance ≠ movement backend — independent axes; **the pawn keeps the full Mover stack. We STAY on Mover. CMC is NOT used** (CMC would only come via Option C, a separate pawn-level swap, never forced by the anim choice).
- **`UAZ_InfectedAnimInstance` (gutted) is Mover-based.** It reads velocity via the cached **Mover component** — `Cached_MoverComponent->GetVelocity().Size2D()` — NOT `Actor->GetVelocity()` (returns 0 on Mover pawns). Plus ASC owned tags via `IGameplayTagAssetInterface`. NO PoseSearch/Chooser/Trajectory/BlendStack (the ~70% deleted from the MM copy). ~100 lines: compute the blendspace alpha + `bIsMoving` + foot contact + turn yaw-delta + attack-overlay alpha; that's it.
- **`ABP_DemoPlayable` is the STRUCTURAL TEMPLATE** for `AZ_ABP_Chalkie` (full audit: [[reference_zombie01_abp_demoplayable]]). Its pose-graph SHAPE is the keeper — blendspace locomotion + optional turn-in-place SM + upper-body overlay; its LOGIC almost all dies because that logic is "read a PlayerController axis + CMC," which Mover+AI+GAS replace.
- **Translation map (4 buckets):** (1) **Stays AnimGraph** — the whole pose graph (3 SMs / blendspace / blends); C++ can't define pose links. (2) **Becomes C++** in `UAZ_InfectedAnimInstance` — the driver-variable math (speed→blendspace, turn yaw-delta, foot contact). (3) **Mover replaces** — `GetFwdAxisValue(Controller)`→Mover speed; `SetMaxWalkSpeed`/"Fwd Vel" curve and "Lateral Dist" mesh-offset hack → deleted. (4) **GAS replaces** — `Attacking`/`AttackType`/death/hit/phase → ability tags + **montages into a slot** (RM via `FLayeredMove_AnimRootMotion`), NOT the demo's in-graph Attacking SM polled from a controller.
- Build rule: new UCLASS → editor-closed CLI build + restart (not Live Coding).

## Tooling note (CORRECTED 2026-06-27 — both servers live; the 2026-06-26 "unrealclaude gone" note was WRONG)
**BOTH MCP servers are connected.** The earlier claim that unrealclaude was gone/minimal was a misread of the `mcp__unrealclaude__unreal_status` tool's BAKED DESCRIPTION (it literally says "Currently: NOT CONNECTED" as static text) — CALL the tool live and it returns `connected:true`. This false assumption cost time (I almost rebuilt a measurement harness that was available all along).
- **`unrealclaude` — FULL surface, deferred (load via ToolSearch `+unrealclaude ...`):** `unreal_execute_script` (**cpp** = the foot-in-air / root-motion offline-C++ technique — writes VERBATIM to `Source/AZ/Generated/UnrealClaude/<desc>.cpp` + Live-Coding-compiles, run from an anon-namespace file-scope static initializer; **python**; **console**), `unreal_blueprint_query`, `unreal_get_output_log` (filter e.g. a UE_LOG tag), `unreal_asset_search`, `unreal_ue` (domains blueprint/anim/character/enhanced_input/material/asset — anim has `set_blend_space`/`set_state_anim`/SM ops), spawn/move/delete/set_property, asset_dependencies/referencers. The AZ_*Utils libs run via `unreal_execute_script python`. **Gotcha: a failed cpp script leaves its `.cpp` in Generated/UnrealClaude/ and it recompiles (and re-fails) every build → DELETE stale probe files between attempts + after.**
- **`unreal-mcp` ModelContextProtocol toolsets (ADDITIONAL, added cdf2a57)** — call via `mcp__unreal-mcp__call_tool` (`toolset_name`+`tool_name`). Useful: `...object.ObjectTools` (list/get/**set_properties — `values` arg is a JSON STRING**), `...blueprint.BlueprintTools` (read/`write_graph_dsl` [merges/keeps un-redefined events], `compile_blueprint`), `...asset.AssetTools` (`save_assets` takes `asset_paths`[]), `...skeletal_mesh.SkeletalMeshTools`, `...scene.SceneTools`, `EditorToolset.EditorAppToolset` (PIE start/stop, CaptureViewport, SearchCVars — but NO console-command exec → can't fire LiveCoding.Compile from here; use unrealclaude execute_script). `ProgrammaticToolset` sandbox = math/json/re only, NO `unreal` module.

Build rule reminder: new UCLASS/UPROPERTY/UFUNCTION → editor-closed CLI build (`Build.bat AZEditor`) + restart; body-only → Live Coding. See [[project_v2_architecture]], [[project_multipawn_class_design]], [[project_sp_first_coop_extensible]] (homing's GetPlayerPawn(0) is SP-only scaffolding; perception is per-controller/co-op-safe).
