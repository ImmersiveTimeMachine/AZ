---
name: project_v2_locomotion_progress
description: "Live progress + resume point for the v2 chooser-driven locomotion build (feature/rootmotion). Iteration ladder Idle→Walk→MM→directions→gaits→starts→stops; where we are, decisions made, and the exact next action."
metadata: 
  node_type: memory
  type: project
  originSessionId: 787f844b-69e1-48c0-8b39-9a9264829d57
---

v2 locomotion build on `feature/rootmotion`, AnimInstance `UAZ_MoverAnimInstance` + chooser `CHT_v2_CharacterAnimations` + pawn `AAZ_PawnMoverHeroCharacter`. Iterative, PIE-verified each step. See [[project_v2_architecture]], [[project_root_motion_mode]].

## Iteration ladder (target)
Idle ✅ → Walk-loop ✅ → intent IsMoving ✅ → walk STOP (TransitionToIdle, RM + foot-aware) ✅ → walk START (TransitionToLocomotion) ✅ → **run gait** (Movement.* tag → Mover speed) ✅ → run loop/start/stop rows ✅ → **cross-clip walk↔run MM loops** (PoseSearch DB) ✅ → MP anim replication (deferred) → directions → sprint gait → MM-on-starts/stops.
(Starts/stops are the `feature/rootmotion` payoff. As of 2026-05-31 the loop is motion-matched across walk+run by trajectory — smooth gear change.)

## ⚠⚠ RESUME HERE — 2026-06-01 session (turn-starts → reversal → pivots → gait-gate → jump → RM-jump WIP)
**Branch `feature/rootmotion`; committed at end of session (C++ + BPs + new assets PSD/PSS/BP_AZ_GA_Run).**

### DONE & PIE-verified this session
- **Turn-starts 90/135/180 (idle→move):** new `EAZ_StartDirection` enum {Fwd,L90,R90,L135,R135,L180,R180} + `FAZ_v2_ChooserContext::StartDirection`. `BucketStartDirection(signedAngle)` static (45/112.5/157.5 thresholds, +right=R). `PendingStartAngleDeg = FindDeltaAngleDegrees(facingYaw, desiredMoveYaw)` each moving frame; **LATCHED** at the `IdleLoop|IdleBreak → TransitionToLocomotion` edge (the RM turn clip collapses the live angle as it plays). Chooser rows 13-24 (walk+run × L90/R90/L135/R135/L180/R180 → `RTG_RM_{Walk,Run}FwdStart{90,135,180}_{L,R}`); forward rows 8/10 = `=Fwd`, all other existing rows = Any. Sign convention verified (no mirror).
- **Reversal = direct loop→pivot (skip the stop):** `bHardTurn = |PendingStartAngleDeg| >= 135.f` (const, tunable). In `LocomotionLoop`, a hard turn routes STRAIGHT to `TransitionToLocomotion` (turn-start) — no plant-stop; the turn-start RM overrides the velocity. (An earlier committed-stop→start version felt laggy; user wanted direct.) Added RM-move **cancel-before-queue** in NativeUpdateAnimation so chained transitions don't double-drive.
- **Moving run-180 pivot (RunFwdTurn180):** new `bMovingTransition` field + `bLatchedMovingTransition` (latched **true** when `TransitionToLocomotion` entered from `LocomotionLoop`, false from idle). Chooser BoolColumn + 4 rows (run, L180/R180, bMovingTransition=True, bLeftFootDown True→_RU / False→_LU) → `RTG_RM_RunFwdTurn180_{L,R}_{RU,LU}`. From-idle run-180 rows (23,24)=False; walk-180 & run-135 fall back to from-idle clips (Any — no moving-pivot assets for those).
- **Gait-gate loop MM:** REMOVED the `LocomotionLoopDatabase` (walk+run) override in `SetBlendStackAnimFromChooser`; loop MM now searches only the chooser-picked **gait** clip → run stays run through a fast turn (no walk downgrade when the turn cuts forward speed). Gait is tag-driven so cross-clip-by-speed was wrong. `LocomotionLoopDatabase` UPROPERTY+CDO kept (still cooks) but unused. Stripped `[v2 MM?]` logs.
- **Falling-mode facing fix:** `AZ_PawnMovementMode_Falling::GenerateMove` — when `OrientationIntent.IsNearlyZero()` (jump-in-place / no air input), early-return (keep liftoff yaw) instead of steering to `ZeroVector.Rotation().Yaw==0` (world-0 = "snaps to initial rotation" bug). Only steer toward intent when air-controlling.
- **Jump arc (physics, split) — BUILT then SUPERSEDED:** `bJustLanded` field + `bLatchedJustLanded`. DeriveSMState air: ground→air=`TransitionToInAir`(JumpIdleStart)→`InAirLoop`(FallingLoop)→air→ground=`TransitionToIdle`+bJustLanded(JumpIdleLand). Chooser bJustLanded BoolColumn + 3 rows (29 takeoff, 30 loop, 31 land). **Now mostly bypassed by the RM-jump experiment** (rows 30/31 dead; row 29 repointed to `_ALL`).

### 🚧 RM JUMP — WIP, BLOCKED (the **tomorrow** task)
**Goal:** drive the whole jump by the clip's root motion (`RTG_RM_Jump_place_ALL`), not physics. **State (in source, Live-Coding-patched only):**
- Pawn `AAZ_PawnMoverHeroCharacter`: `SetHandleJump(false)` in **ctor + BeginPlay** (suppress the physics jump; BeginPlay set is archetype-proof).
- `DeriveSMState`: jump triggered off **`bIsJumpJustPressed`** read from the input cmd (GA still ships it via `SetJumpPressed`; Mover no longer consumes it) — NOT MovementMode. `TransitionToInAir` is a single **HELD** state (decoupled from MovementMode) that plays `RTG_RM_Jump_place_ALL` for the clip length, then settles. (An earlier grounded-settle anti-restart was REMOVED — user gates re-trigger in `BP_GA_Jump`.)
- Chooser row 29 → `RTG_RM_Jump_place_ALL`. **RM bridge UN-GATED** for the jump (the `bAirTransition` skip in `SetBlendStackAnimFromChooser` was removed → the jump transition queues `FLayeredMove_RootMotionAttribute` like stops/starts).
- **BLOCKER (diagnosed by user):** the clip HAS vertical Z root motion (the anim editor shows the lift), but in **Walking mode the floor-snap eats the upward Z** → capsule stays grounded. RM-disabled jumps fine (no OverrideAll pin); physics-on lifted only because the impulse kicked us into **Falling** (vertical-permitting). Plain flip-to-Falling won't work either: the clip's **anticipation crouch** (~0 up-Z at the start) would immediately re-ground.
- **DECISION — do tomorrow: build a dedicated RM-action movement mode** (no gravity, no floor-snap; just follow `FLayeredMove_RootMotionAttribute` X/Y/**Z** for the jump's duration, hand back to Walking when the clip ends). GASP-style RM-traversal mode; reusable for vault/mantle later. (Option-2 "re-enable SetHandleJump to confirm Falling passes Z" was NOT done — user chose option 1, the mode.)

### Chooser CHT_v2_CharacterAnimations — **32 rows**, columns REORDERED on reload (see [[feedback_chooser_column_reorder]])
`0 SMState | 1 Stance | 2 Gait | 3 MovementDirection | 4 bLeftFootDown(Bool) | 5 Randomize | 6 StartDirection(Enum) | 7 bMovingTransition(Bool) | 8 OutputStruct | 9 bJustLanded(Bool)`. context_count=3. **Always re-inspect indices before any SetCell-by-index.** New context fields this session: `StartDirection`, `bMovingTransition`, `bJustLanded` (all reflected → each needed a full build).

### Binary / build state (IMPORTANT for resume)
Last full CLI build = the `bJustLanded` jump build (physics-split arc). Everything after it — **gait-gate, falling-facing fix, the entire RM-jump experiment** — is **Live-Coding-only → dies on editor restart**. So on tomorrow's reopen the editor runs the bJustLanded binary until a fresh full build. Source on disk (committed) is ahead. Tomorrow: write the RM-action mode → full CLI build (editor closed) → restart → wire/test.

---

## Done (verified in PIE)
- **Iter 1 walk loop**: chooser row LocomotionLoop+Standing+Walk(equal)+Dir=Any+bLeftFootDown=Any → `RTG_RM_WalkFwdLoop`, `bUseMM=False`, BlendTime 0.3. Velocity-driven (walking mode), RM extracted+discarded (loops in-place). Walk starts/plays. CHT saved.
- **Intent-based IsMoving + MovementDirection**: now read PREDICTED future velocity (leading), not current Speed2D. Walk fires the frame W is pressed (verified). Release flips to idle early (stop-anim gap, expected — stops iteration fills it).
- **Multi-chooser + bLeftFootDown**: `SetBlendStackAnimFromChooser` takes `Candidates` array (MM-over-set fallback to `{ChosenAnim}`); `bLeftFootDown` struct field + BoolColumn (col 6) on chooser. (Typo `bLeftFokootDown` from a linter once broke the build — watch for recurrence.)

## Decisions
- **Trajectory = option A** (predictor-based `FTransformTrajectory`), NOT option B (`GetPredictedTrajectory`). B was a fine IsMoving-only interim but MM's PoseHistory node needs `FTransformTrajectory`; unified on one source (v1's exact approach). B code removed.
- **MM needs schema/database** — `MotionMatch` AssetsToSearch accepts EITHER a `UPoseSearchDatabase` OR an animation asset carrying a `UAnimNotifyState_PoseSearchBranchIn` (engine `PoseSearchLibrary.h` line ~205). ⚠ **CORRECTION (2026-05-31): the earlier "RTG_RM_WalkFwdLoop already has a BranchIn→DB, reuse existing" claim was WRONG** — `unreal_asset_search PoseSearchDatabase` found 178 DBs but ALL GASP's (UEFN_Mannequin, index GASP anims); ZERO indexed our `RTG_RM_*` clips. Our clips are on `SKEL_SurvivalMan`; GASP schema `PSS_Default` is mannequin-skeleton (not reusable); our loops have only `contact_l`/`contact_r` curves (no `Phase`). So MM for our clips = **built from scratch** (see milestone below). The walk loop had been `bUseMM=False` direct-play all along.
- MM recipe (validated vs v1 + engine + GASP): PoseHistory node = `AnimGraphNode_PoseSearchHistoryCollector`, tagged `"PoseHistory"`, placed before Output Pose, its `TransformTrajectory` pin bound to a member named `Trajectory`. C++ each tick: `UPoseSearchTrajectoryLibrary::PoseSearchGenerateTransformTrajectoryWithPredictor(predictorInterface, dt, Trajectory, PredictionYawLast, Trajectory, 0.04,10, 0.1,10)`. `MotionMatch(this, {ChosenAnim}, FName("PoseHistory"), ...)` then picks entry frame.

## ⚠ RESUME HERE (current state 2026-05-31)
**DONE & working in SP-PIE:** Idle, IdleBreak, Walk loop, walk STOP (foot-aware RM, approach A′), **walk START** (`TransitionToLocomotion` → `RTG_RM_WalkFwdStart`, symmetric to stop in `DeriveSMState`), **RUN gait** (speed rises walk→run), **run loop/start/stop rows**, and **cross-clip walk↔run MM** (the loop motion-matches across a walk+run PoseSearch DB by trajectory → smooth gear change). Chooser `CHT_v2_CharacterAnimations` rows 0-12.

### Milestone 2026-05-31 — run gait + cross-clip MM (full recipe)

**Run gait wiring (the chain that turns a tag into speed):** the Mover walking mode `UAZ_PawnMovementMode_Walking::GenerateWalkMove_Implementation` ALREADY maps `EAZ_Gait` → `WalkSpeed`/`RunSpeed`/`SprintSpeed` (165/375/585) via `ResolveGait` reading `FAZ_MoverCustomInputs.Gait`. The v2 pawn just never shipped that struct → gait defaulted to Walk. **Fix:** `AAZ_PawnMoverHeroCharacter::ProduceInput_Implementation` now adds `FAZ_MoverCustomInputs` and sets `.Gait` from the player's **`Movement.*` GAS tags** (`Movement_Sprinting`→Sprint, `Movement_Running`→Run, else Walk), read via `HasMatchingGameplayTag` on the game thread (ASC reachable; sim can't query ASC → translate tag→gait here, ship the resolved enum in the deterministic InputCmd). **`BP_GA_Run` grants `Movement.Running` via ActivationOwnedTags** (user-built; confirmed working — `[v2 MM?]` showed gait flipping 0↔1 with sprint). Decision: drive Mover/anim off **`Movement.*` domain tags, NOT `Ability.State.*`** (which is ability-coordination only — block/cancel); see [[project_gas_gameplay]]. AnimInstance `ChooserContext.Gait` now reads the SAME input-cmd gait (intent), NOT speed thresholds (so a from-idle run-start selects Run rows on frame 1 when speed is still ~0). Includes added to pawn: `Animation/AZ_LocomotionTypes.h`, `AZ_GameplayTags.h`.

**Cross-clip MM build (from scratch — there was NO DB for our clips):**
1. **Schema** `PSS_v2_SurvivalMan_Loco` — DUPLICATED GASP `PSS_Default` then repointed skeleton to `SKEL_SurvivalMan` (Python: read `schema.skeletons` list → `entry.set_editor_property("skeleton", skel)` → set back; NOTE PoseSearchSchema has NO `post_edit_change` and uses `skeletons` plural roled array, not `skeleton`). Reuses GASP's proven Pose+Trajectory channels — standard UE bone names match SurvivalMan, so `BuildIndex Succeeded`. Saved at `/Game/AZ/Blueprints/Animation/MotionMatching/`.
2. **Database** `PSD_v2_Loco_Loops` — `AssetTools.create_asset(..., unreal.PoseSearchDatabase, None)`; `db.set_editor_property("schema", schema)`; `UAZ_PoseSearchUtils.add_sequences_to_database(db, [WalkFwdLoop, RunFwdLoop])`. Indexed OK (DDC). Opening the schema editor afterwards CANCELS the index (`PreCancelled because of PSS...`) — it rebuilds on access; close schema/DB tabs before PIE.
3. **No BranchIn notify path** — the util `AddBranchInNotify` doesn't set the notify's `Database` (and Python can't touch `Notifies` — protected). Instead **pass the DATABASE directly to `MotionMatch`** (engine allows it). C++ `SetBlendStackAnimFromChooser`: when `SMState==LocomotionLoop && LocomotionLoopDatabase`, `AssetsToSearch.Reset(); AssetsToSearch.Add(LocomotionLoopDatabase.Get())`. MM then picks walk-vs-run pose by trajectory = seamless gear change.
4. **DB cookability** — the DB is referenced ONLY by `UPROPERTY(EditDefaultsOnly) TObjectPtr<UPoseSearchDatabase> LocomotionLoopDatabase` on `UAZ_MoverAnimInstance`, **assigned in the `AZ_ABP_MoverAnimInstance` CDO** (creates the asset reference so it cooks; a raw `LoadObject` path leaves it orphaned/uncooked). Graceful fallback: if MM returns null (DB unassigned/empty), direct-play the chooser clip so the loop never freezes.

**🔑 ROOT CAUSE (TRUE FIX 2026-05-31) — output struct column bound to the WRONG CONTEXT.** `ChooserOut.bUseMM` arrived **default-false** at runtime even though rows 5/9 author `bUseMM=True` → `if (ChooserOut.bUseMM)` never fired → `MotionMatch` never reached → loop direct-played (snapped). **The real cause:** the OUTPUT struct column (col 6, `FOutputStructColumn`) had `InputValue.FStructContextProperty.Binding.ContextIndex = 1`, but `AZ_ChooserOutputs` sits at **context[2]** in the 3-context CHT_v2 (`[0]` AZ_MoverAnimInstance, `[1]` AZ_v2_ChooserContext, `[2]` AZ_ChooserOutputs). So the matched row's struct was written to context[1] (the redundant `AZ_v2_ChooserContext`) and never reached the `EvaluateChooser` node's `AZ_ChooserOutputs` output. **The exact OUTPUT-side twin of the `bLeftFootDown` wrong-context bug** — the stray middle context [1] is the shared culprit. Source bug: `AZ_ChooserUtils.cpp` `AddOutputStructColumnToSub`/`ConfigureSubTableColumns` hardcode `ContextIndex = 1` (stale "2-context" comment "Context[1] = ChooserOutputs"); the 3-context layout needs 2. **FIX (persists — it's in the saved chooser asset): extended `SetColumnBindingChain` to handle `FOutputStructColumn`, then `set_column_binding_chain(CHT_v2, 6, [], 2)` + `compile_and_save`.** Survives restart (saved asset + base binary's `if (ChooserOut.bUseMM)`), unlike a Live Coding patch.
⚠ **RED HERRINGS that wasted hours (don't repeat):** (1) "multi/return-all mode doesn't populate the output struct → switch to first-match" — WRONG; first-match did NOT fix it (bUseMM still 0 after restart + ABP recompile). (2) The in-session "bUseMM=1 now" was actually the `effMM` **force** (`bEffectiveUseMM = bUseMM || (LocomotionLoop && DB)`) masking the real bug — user rightly rejected the force; the true fix is the context rebind. (3) "stale compiled ABP" — recompile+save the ABP did NOT fix it. Python can't read column bindings (`FInstancedStruct` protected) — had to find `ContextIndex=1` in the C++ util **source**. **DEFERRED cleaner fix:** delete the redundant `AZ_v2_ChooserContext` context[1] entirely (2-context layout → bundled `ContextIndex=1` becomes correct; removes the trap that caused BOTH bugs). Ripples to ABP: `EvaluateChooser` loses `AZ_v2_ChooserContext_*` pins → the advisory `State` wire drops (harmless — `DeriveSMState` is truth); then output col `2→1`. User parking this for later w/ a TODO on `FAZ_v2_ChooserContext`.

**🔧 BUILD INFRA — see [[feedback_build_paging_file_parallelism]]:** the new `UPROPERTY` forced a full CLI build; Live Coding's adaptive rebuild hit `C3859 / paging file too small / C1076 internal heap limit` (32-action rebuild, ~17 parallel cl.exe). Fix = cap parallelism in `BuildConfiguration.xml` (`MaxParallelActions=4`, `bAllowXGE=false`, `ParallelExecutor MaxProcessorCount=4`). Also: Live Coding patches DON'T survive an editor restart — on reopen you get the last full-build binary (lost this session's patches once; a full CLI build re-baked everything).

**Debug still in (gated by `bDebugTrajectory`):** `[v2 Trajectory]`, `[v2 Foot]`, `[v2 TRANS]` overlays/logs. `[v2 MM?]` stripped 2026-05-31.

**NEXT — MP anim replication (analysis done 2026-05-29; not yet implemented):** in MP the replica capsule MOVES (Mover replicates) but anim stays idle, because we switched `bIsMoving` to read `CachedMover->GetLastInputCmd()` and input is NOT sent to **simulated proxies** (other clients) by default — only owning client (local) + server (input replayed). **FIX = set `bSyncInputsForSimProxy = true` on the Mover component** (UPROPERTY on UMoverComponent, AdvancedDisplay/Experimental) — engine flag built for exactly "input used to hint at object state, such as an anim graph"; ships input to sim proxies via sync state so `GetLastInputCmd()` works there. Everything else already replicates: Mover sync state (speed/mode/stance/gait), GAS OwnedTags, local curve eval (bLeftFootDown), predictor/MM (from replicated velocity + local PoseHistory). Authoritative RM transition runs server-side (has input); sim proxies play the clip visually + get capsule from replication (their local layered-move queue is redundant). Idle-break/transition timers are local (cosmetic, OK). Alt = revert bIsMoving to velocity/trajectory-based (replicated free) but reintroduces the SP stop-lag — so the flag is preferred. Decide: set flag in component ctor vs BP default. Aligns with [[project_sp_first_coop_extensible]].

**Cleanups pending a build:** (1) `SetColumnBindingChain` now takes `ContextIndex` (default 0) + sets it — DONE in code, needs CLI build to go live (tooling-only, chooser already fixed in-editor). (2) Strip `[v2 STOP]` + `[v2 Foot]` debug logs in `AZ_MoverAnimInstance.cpp`. (3) Visual-confirm foot mapping (True→_RU / False→_LU) — flip rows 6/7 if it skates.

## Transitions (NEXT) — research findings 2026-05-29
**Anims exist** at `/Game/Assets/RM_Movement/` (RTG_RM_ prefix), foot-tagged `_LU`/`_RU` (left-/right-foot-up) → maps onto our `bLeftFootDown` BoolColumn:
- Starts: `RTG_RM_WalkFwdStart`, `RTG_RM_RunFwdStart`, `RunFwdStart90_L/R`, `RunFwdStart180_L/R`.
- Stops: `RTG_RM_RunFwdStop_LU/_RU` (walk stops likely present too — confirm names when building).
- Turns: `RTG_RM_TurnLt/Rt90_Loop`, `TurnLt/Rt180`, `RunFwdTurn180_L/R_LU/RU`.
- Catalog gap (reference_noweapon_anim_catalog): no sprint stop, no backward run stops, no slide exit.

**RM-gate mechanism — authoritative read of `RootMotionAttributeLayeredMove.cpp` (engine):**
- Layered move contributes ONLY when the `RootMotionDelta` mesh attribute is present. `RootMotionFromEverything` writes it EVERY frame (loops too, ≈zero) → that's the W-deadlock source under persistent OverrideAll.
- Skip condition (line 137-140): `if (!bHasValidWarpingContext && (InputCmd->bIsJumpJustPressed || MoverComp->HasGameplayTagInState(SyncState, Mover_SkipAnimRootMotion, false))) return false;`
- **`Mover.SkipAnimRootMotion` is a SYNC-STATE tag, NOT the ASC tag.** It is only declared+read in the Mover plugin (never set by default modes — air control uses the `bIsJumpJustPressed` half). So it's an external hook for game code.
- **`UMoverComponent` tag API**: `AddGameplayTag/RemoveGameplayTag` (→ `ExternalGameplayTags`), `HasGameplayTagInState` (→ walks sync state: mode/modifiers/layered moves via `CollectGameplayTagsFromSyncState`). ⚠ UNVERIFIED whether `AddGameplayTag`'s external tag reaches `HasGameplayTagInState`'s sync-state check — may need a movement-mode/modifier to contribute the tag. VERIFY FIRST if going approach A.
- **Line-137 montage escape hatch**: when a root-motion MONTAGE is playing, `bHasValidWarpingContext=true` → RM is NEVER skipped (always drives capsule), tag irrelevant.

**Three viable mechanisms for transition RM → capsule (FORK, user to choose):**
- **A — persistent LayeredMove + skip-tag during loops** (orig memory plan): `RootMotionFromEverything` + queue move once in BeginPlay + add `Mover.SkipAnimRootMotion` to sync state during loops, remove during transitions. Uniform blend-stack+MM pipeline. RISK: tag-injection path (above) needs solving.
- **A' — queue LayeredMove PER-transition** (recommended): keep `RootMotionFromEverything`; queue `FLayeredMove_RootMotionAttribute` only on transition-enter (duration = clip length, auto-expires); loops queue nothing → attribute written but unconsumed → velocity-driven. NO tag, NO montages, transitions stay blend-stack+MM. RISK: queue timing from pawn/mode reading AnimInstance SMState.
- **B — montage transitions + `RootMotionFromMontagesOnly`**: loops write no attribute (clean velocity-driven); transition montages write attribute + warping context → escape-hatch drives capsule. NO tag. Against v2 "no montages" lean; loses MM entry-frame matching on transitions.

**Recommended order: STOPS first** (foot-aware via bLeftFootDown already built; biggest feel win; forgiving to tune vs starts which can fling).

### T1 — walk-fwd STOP (`TransitionToIdle`): C++ WRITTEN 2026-05-29 (uncommitted, pre-build). Chosen mechanism: **A′**.
`AZ_MoverAnimInstance.h/.cpp` edits:
- New members: `TransitionEndTime` (UPROPERTY), `TransitionAlmostCompleteThreshold=0.15`, `bPendingTransitionRMMove`+`PendingTransitionRMMoveDurationMs` (plain — worker→game handoff). **Reflected → needs full CLI build + restart, NOT Live Coding.**
- `DeriveSMState`: moving→idle routes LocomotionLoop→TransitionToIdle→IdleLoop, held by `TransitionEndTime` (default Now+1.0s on entry as a no-row safety net, overridden by real clip length). bIsMoving mid-stop → straight back to LocomotionLoop. Clears `TransitionEndTime` in InAir/moving branches.
- `NativeUpdateAnimation`: (top) if `bPendingTransitionRMMove`, queue `FLayeredMove_RootMotionAttribute` with `DurationMs=PendingTransitionRMMoveDurationMs`; (bottom) on transition→non-transition edge, `CachedMover->CancelFeaturesWithTag(Mover_AnimRootMotion, false)` (handles early-abort + normal completion before auto-expire).
- `SetBlendStackAnimFromChooser`: **transition lock** (if in a transition AND SMState==LastPushedSMState && !bForceBlend → return; stops the stop clip's own contact_l flipping bLeftFootDown and restarting the stop on the other foot); transition-end block sets `TransitionEndTime = Now + (Len - StartTime) - threshold` and flags the pending RM move.
- Include added: `DefaultMovementSet/LayeredMoves/RootMotionAttributeLayeredMove.h`.
**Engine facts confirmed:** `FLayeredMoveBase::DurationMs` is public `double`. `QueueLayeredMove(TSharedPtr<FLayeredMoveBase>)` returns void (no handle → cancel by tag). `CancelFeaturesWithTag` cancels layered moves (engine precedent: PlayMoverMontageCallbackProxy.cpp:63 cancels `Mover_AnimRootMotion_Montage`).
**Chooser rows ADDED 2026-05-29 (verified via dump, in-memory — needs Ctrl+S):** rows 6,7 on CHT_v2 root: `TransitionToIdle+Standing+Walk+F`, BlendTime 0.15, bUseMM=False; row6 bLeftFootDown=True→`RTG_RM_WalkFwdStop_RU`, row7 False→`RTG_RM_WalkFwdStop_LU`. Added via `add_empty_row_to_sub(path,"",asset)` + `set_cell_enum_on_sub(...,"TransitionToIdle"/"Standing"/"Walk"/"F",0=Equal)` + `set_cell_bool_on_sub(...,6,1/0)` + `set_cell_output_struct_field_on_sub(...,4,"BlendTime"/"bUseMM",...)`. SubTableName "" = root (ResolveTable). Enum strings = AUTHORED names (GetValueByNameString), NOT display names.
**BUILT + restarted 2026-05-29; rows live.** First PIE showed a STUCK/oscillating loop (never cleanly stops — observed re-entering WalkFwdLoop).
**ROOT CAUSE + FIX (2026-05-29, Live-Coding-patched, body-only):** `bIsMoving` was computed from `PredictedFutureVelocity` (the predictor). During an RM stop the capsule still moves forward, and the walk loop's velocity bleeds off slowly, so velocity-based `bIsMoving` lags/stays true → SM never leaves LocomotionLoop / oscillates. **Fix:** drive `bIsMoving` off ACTUAL INPUT — `CachedMover->GetLastInputCmd().InputCollection.FindDataByType<FCharacterDefaultInputs>()->GetMoveInput_WorldSpace()`, `bIsMoving = Size2D > 0.1`. Now release→stop fires the frame W is released; RM stop drives the decel. `PredictedFutureVelocity` still used for MovementDirection + MM trajectory only. Include added: `MoverDataModelTypes.h`. **General lesson: intent-based IsMoving must key on INPUT/accel, not predicted/measured velocity — velocity feedback (RM, momentum) corrupts the moving decision.** GASP "future velocity + accel" = the accel/input term is what makes stops work.
**FOOT-SELECTION ALWAYS PICKED _LU — root cause + fix (2026-05-29):** the chooser's `bLeftFootDown` BoolColumn read the column DEFAULT (False) every frame → always row7 (_LU). Cause: its `PropertyBindingChain` was a SINGLE element `["bLeftFootDown"]` (from `AddBoolColumnToSub`), but the working enum columns bind `["ChooserContext","bLeftFootDown"]` (2-element, through the AnimInstance's `ChooserContext` struct member; context[0]=AnimInstance). A 1-element chain tries to read `bLeftFootDown` directly off the AnimInstance → doesn't resolve → default. Confirmed via probe: `RebindChooserPropertyNames` chain[0]=='ChooserContext' matched 4 enum cols, 'bLeftFootDown' matched only the bool col. **CONTACT CURVES WERE FINE** (contact_l/contact_r on RTG_RM_WalkFwdLoop swing 0↔1; double-support overlap where both=1 is normal walk). **GENERAL GOTCHA: the `AZ_ChooserUtils.AddXColumnToSub` adders create single-element binding chains that DON'T resolve for an AnimInstance-context chooser — fields under a context struct member need the member as chain[0].** **FIX:** new `AZ_ChooserUtils::SetColumnBindingChain(path, colIdx, [names])` (sets full chain on any Enum/MultiEnum/FloatRange/Bool column + `Compile(true)`); `AddBoolColumnToSub` now also splits a dotted `PropertyName` ("ChooserContext.bLeftFootDown") into a multi-element chain. Applied: `SetColumnBindingChain(CHT_v2, 6, ["ChooserContext","bLeftFootDown"])` → verified chain[0]=='ChooserContext'==5. (Needed a CLI build for the new UFUNCTION; that build also baked in the LC-patched input-intent bIsMoving fix + foot debug overlay + all T1 transition C++.) **TODO: add SetColumnBindingChain + this gotcha to the az-cpp-utility-tools skill.**
**ACTUAL ROOT CAUSE (RESOLVED 2026-05-29) — bool column bound to the WRONG CONTEXT:** `CHT_v2_CharacterAnimations` has 3 context params: [0] `AZ_MoverAnimInstance` (Class, Input/Output — the real input; columns read its members), [1] `AZ_v2_ChooserContext` (Struct, **Output** — redundant/unpopulated standalone struct), [2] `AZ_ChooserOutputs` (output struct). Working columns bind via context[0]: chain `["ChooserContext","<field>"]` (e.g. Gait header reads "Chooser Context.Gait"). The bool column (from `AddBoolColumnToSub`, 1-element chain `["bLeftFootDown"]`) auto-resolved against context[1] (the struct HAS a `bLeftFootDown` member) — but context[1] is Output/unpopulated → always read default False → always `_LU`. My `SetColumnBindingChain(["ChooserContext","bLeftFootDown"])` then ERRORED (`LogChooser: Property ChooserContext not Found on AZ_v2_ChooserContext`) because it set the chain but NOT the ContextIndex — still pointing at the struct, which has no `ChooserContext` member. **FIX THAT WORKED: bind in the chooser EDITOR UI via `AZ_MoverAnimInstance → ChooserContext → bLeftFootDown`** (sets BOTH ContextIndex=0 AND the 2-element chain + caches the resolved property). Header then reads "Chooser Context.bLeftFootDown" like Gait. Foot selection now works (bLeftFootDown=1 → _RU). **LESSONS: (1) `AZ_ChooserUtils.AddXColumnToSub` 1-element chains are a footgun for AnimInstance-context choosers — they can auto-bind to a stray struct context. (2) `SetColumnBindingChain` is incomplete — must also set ContextIndex (TODO: fix it). (3) The EDITOR UI bind is the reliable path (resolves+caches); programmatic chain-only edits don't reliably resolve at runtime. (4) context[1] AZ_v2_ChooserContext is redundant/Output — a deletion candidate but shifts indices.** FOLLOW-UPS: fix `SetColumnBindingChain` to set ContextIndex; strip `[v2 STOP]`+`[v2 Foot]` debug; verify foot mapping visually (flip rows 6/7 if it skates).

**GOTCHA #2 (superseded by above — was a wrong lead) — programmatic chain change needs a fresh LOAD to resolve at runtime:** after `SetColumnBindingChain` set col6 to `["ChooserContext","bLeftFootDown"]` (+`Compile(true)`), the `[v2 STOP]` log proved `ctx.bLeftFootDown` VARIES correctly (1 and 0) but the chooser STILL chose `_LU` every time → it kept reading the bool as default False. `Compile(true)` updated the asset data but did NOT refresh the live binding resolution in-session (enum columns resolve their chains at LOAD). **FIX: Ctrl+S the chooser → restart the editor → bool binding resolves on fresh load like the enum columns.** (So `SetColumnBindingChain` is correct for authoring, but its effect only goes live after a save+reload, not hot.)
**MAPPING (confirmed by user intuition):** right foot up (`bLeftFootDown=1`, left planted) → play `_RU` to plant the right and finish the step → row6 (True→_RU). So mapping is correct; verify visually after the binding resolves.
**STILL PENDING:** (1) Ctrl+S + restart editor; (2) re-test stops — expect `_RU` when bLeftFootDown=1, `_LU` when 0; (3) remove the `[v2 STOP]` + `[v2 Foot]` debug once confirmed. NOTE `bLeftFootDown = contact_l > 0.5` is crude in double-support — possible refinement to `contact_l > contact_r`. MM-on-stops + STARTS (TransitionToLocomotion) are next.

## Files touched (uncommitted)
`AZ_MoverAnimInstance.h/.cpp` (trajectory + intent IsMoving), `AZ_PawnMoverHeroCharacter.cpp` (predictor Setup; RM LayeredMove removed), `AZ_LocomotionTypes.h` (bLeftFootDown), `CHT_v2_CharacterAnimations` (walk row + bLeftFootDown col — saved).
