---
name: CHALK unified menu and indicator studies
description: Field Notes selected and being integrated across CHALK; saved asset progress, staged native input/menu work and final-only runtime acceptance.
type: project
---

## September20 decision

## September21 — current handoff

**PAUSED AT ARTUR'S EXPLICIT REQUEST.** Stop implementation for today. All helpers have completed; no build is running. Resume only after Artur asks to continue. The earlier close-editor request is superseded by this pause; do not treat a late answer to it as permission to resume work. Read [the complete resume checkpoint](C:/UnrealEngine/Games/AZ/docs/design-briefs/field-notes-resume-handoff.md) first next session. Exact boundary: 30-file native batch staged, source_applied=false, build not run; previous asset work saved; future prompt/keycap/menu/world-caption assets await the new build. No reminder or automatic continuation scheduled.

FINAL PREPARATION COMPLETE: vitals delivered WorldInteractionPromptNativeProposal README + author_world_prompts.py + asset-plan.json; native nine files frozen. PromptHosts v3 includes live DefaultRootWidget==None precreation guard and external native creation split. Root reconfirmed merged world files are text-identical (raw hashes differ where original CRLF is preserved), regenerated the 30-file batch and passed git apply --check --whitespace=error-all. No source applied/build. Post-build recipe adds one HUD prompt and performs explicit caption audit/migration, including exact four quest fixture captions; do not claim custom caption coverage until that audit completes. All helpers completed. User asked status again; current process check confirms UnrealEditor52888 still open. Existing close request is the remaining build blocker.

BUILD READY, WAITING FOR EDITOR CLOSE: latest 30-file combined patch passes applicability/whitespace checks. Independent peer review found no further world-prompt issues and confirmed the saved owned HUD has no ShowPickupMessage/HidePickupMessage Blueprint override being bypassed. Vitals was told to freeze native source and finish only the world asset/caption recipe. Latest process check still shows UnrealEditor52888; user asked status, root explained close/restart boundary. No source applied and no build started. Existing async close request remains pending; do not ask a duplicate permission question. After user closes: recheck process/source/proposal hashes, apply reviewed CombinedNative, normal build/repair compiler errors, record actual result; never auto-launch editor. Details in CombinedNative/review-checkpoint.json. Runtime test only after remaining assets are wired.

LATEST BUILD PREPARATION: CombinedNative now stages 30 files (10 new, 20 modified), including WorldInteractionPromptNativeProposal's nine existing files merged with MenuRoutes PC edits. No live Source applied, no build. World HUD uses the actual Input.Action.Interact action; current E binding has no gamepad counterpart, so an invalid current-device key uses readable full-width Unbound + caption (shared prompt default hides unbound glyphs). Magazine captions retain live rounds/capacity, quest/item custom captions are explicit fields, no localized-string prefix stripping. World source peer review and HUD/caption migration recipe are finishing. PromptHosts is now v3: prepare_prompt returns an external native creation request; author_prompt validates the fresh empty Blueprint and builds it. CreateWidgetBlueprint may compile internally, so never call it through Python. Installed DefaultRootWidget is null with no saved override; a live read guard precedes creation. User was asked to close Unreal for the upcoming common build; no reply yet and last observed PID52888 still running. Dirty content/maps were both empty before asking. User alone reopens Unreal after successful build. Full runtime check still deferred until integration complete.

Newest refinement: CombinedNative prepare succeeded for 23 files (10 new, 13 modified), source_applied=false. InventoryNavigation v2 handles pending destination during fades, visible-arrival focus and closed-parent late-callback guards. MenuRoutes final proposal includes held-Boolean barrier, explicit class fallback, correct Map focus and both actual prompt variants. PromptHosts v2 avoids inventory footer Q/E duplication and shows parent section prompts on Map. Helper vitals now owns two outstanding preparation items: split prompt creation into an external native tool call (creation internally compiles), and stage adaptive world interaction hints from the existing Module7 scope. No new live Source or editor mutations in this refinement.

Field Notes remains the approved full implementation. User added visible previous/next controls for inventory navigation: Q/E on keyboard, actual mapped gamepad glyphs (currently LT/RT, or L2/R2). Four logical sections are Equippables, Consumables, Craftables, Map, with reverse/wrap. InventoryNavigationNativeProposal contains four staged source files and a six-widget additive asset recipe. Physical grid order differs from the visible order; navigation and labels now use widget identity in the proposal. No new inventory input bindings are registered by the keycap widgets.

Saved assets: all 23 inventory first-pass styles, linear vitals, center/vitals/skills layout corrections, map/journal style and disclosure layout, HUD/Quick/compass styles, shared resources and ten textures. Journal details are initially collapsed inside an expandable section, retaining the existing data/track controls. The latest journal preview is design-only; runtime acceptance remains pending. The user explicitly wants testing after all integration is complete.

Native work is still staged, not applied or built: journal presentation, Quick/Map input, adaptive prompts and local UI/audio preferences, menu routes, and inventory navigation. Combined staging now includes the navigation manifest; regenerate it after menu review finishes. Current live native changes remain the previously built three inventory style metadata flags. Root alone writes editor assets. Helpers: visual_review owns final menu corrections; vitals coordinates prompt hosts with the new tab-row controls; skills delivered navigation. No new PIE or automated tests.

Input review correction: the engine FlushPressedKeys path marks Boolean mappings ignored, then simulated releases through EnhancedPlayerInput clear those flags again. The enabled flush CVar alone does NOT establish safe resume behavior. Menu helper is implementing a project-owned transient mapping barrier using public APIs; no engine edit or simulated key injection is authorized by this approach. Also fixed in the staged menu proposal: explicit widget-class fallback and avoiding parent focus stealing from MapCanvas.

Current input assets are saved: 15 UI command actions, two Quick/Map contexts and generic action metadata; Map actions do not consume Enhanced Input mappings so parent reserved-key checks remain truthful. AZ UIInputData now points Click/Back to owned mapped actions. Config registers XSX/PS4/PS5; actual active configuration refresh awaits the next normal build/restart. Shared prompt hosts and menu/keycap assets must wait until their native types are loaded. Save owned packages and ask for editor close only when the combined batch is ready; user launches Unreal himself. Do not ask for intermediate Play checks.

The chronological checkpoints below are history; this section takes precedence.

CURRENT: metadataDLLreopenedPID52888; composite recoveryexecuted; all23inventoryfirstpassstylescompiledverifiedsaved (newplanhasha6d93...afterCaption_Storyone-refmigration). CommonTextBlock synchronizationderivedfields validatedagainstassignedstyleCDO (font/strike/margin/lineheight/color/shadow) afterengineUpdateFromStyle evidence. Structuralfinish+center/vitals/skillslayoutfixesALLappliedcompiledsaved. Screenshot025427inventorypreviewhasaligned3columns/noclipping; designstateonlygrid/healthnotruntime. Neverrerunfirstpassstrict-treeverifyafterstructuralfinish. UserexplicitlywantsPIEonlyafterALLintegrationfinished. Sharedstyles19/art10; PS5CreateC64nativeGIMPglyphsaved+PS5onebrushdelta; ConfigXSX+PS4/PS5registrationAPPLIEDbutactivecachenotverifieduntilnextrestart. NextnativeproposalsSTAGEDonly:Journal4,Quick3/Map2,Prefs5new; MenuRoutespreparing. Peerreviewfixesunderway:balancedQuickreleases/Mapconflicts+handlehook, invalidatedpromptordering, currentuserdevicepairing. Rootonlyeditorwriter. Helpersfieldnotes_visual_review=MenuRoutes,fieldnotes_vitals=QuickMapfix,fieldnotes_skills=Prefsfix. Lastsourceappliedremains3stylemetadataflags2headers; normal17actionbuildpassed.

NEWEST: inventorymetadata normalAZEditorbuildPASSED17actions14.62s, DLLlinked/UHTcompleted; sourcehashesmatched exact2headers/3styleflags. Userclosededitor; rootdidNOTlaunch. Receipt Saved/FieldNotesImplementation/InventoryMetadata/build-receipt.json nowexists withresultSucceeded+source_sha256. Waitforuserreopen(orreadhealthonly); thenTools/field_notes_inventory_metadata.py:resume_after_metadata_build(), resume19inventoryassets, structuralfinish. AllpriorassetworkwascleanlySAVEDbeforeclose. EntireFieldNotesstillinprogress; runtimeacceptancepending.

LATEST BUILD BOUNDARY: Root completed/saved17overlayassets+Quickframes/notch+guardedSidecategoryevent; allnativeBPcompiles/readbacks pass, runtimepending. Map4+art9saved. Inventory4authoredsaved; remaining19blocked on EditInstanceOnly stylefields onWidgetTree templates (engineandPythonbothenforce). ROOT APPLIED3metadataflagchangesonly in2nativeheaders(TextStyle/LabelStyle/ValueStyle→EditAnywhere), backedup/exactdiffproof InventoryMetadata/source-receipt.json, NOTYETCOMPILED. FailedComposite_Accuracy all14objectsunchanged, externallycompiled+saved, dirtycontent/mapsEMPTY. UseraskedcloseUnrealfornormalbuild; neverlaunchEditor. AfterbuildwriteInventoryMetadata/build-receipt.json with resultSucceeded/source_sha256, userreopens, callTools/field_notes_inventory_metadata.py:resume_after_metadata_build(), then remaininginventory+structuralfinish. Earlierresave/planmigrationsalreadyexecuted; don'trepeat. PS4/PS5owneddata32keyseachcompiledsaved; configunapplied,prompt/gamepadimplementationpending. Formerhelpersgoneafterinternetinterruption; rootresumedlocally. NoPIE/testsstarted.

NEWEST SAVED CHECKPOINT: MapPilot completed asset integration:3widgets category/paper/Roboto/selectionmargin compiled+postcompileverified+saved; DA MapTexture assigned preservingallcalibration; all8textures imported viaexplicitTextureFactory/recoveredStory, verifieddimensions/settings/hash, saved. MapPilot/saved.json exact12packages. Runtime/DPI/Back/loadacceptance PENDINGuser. Inventoryfirst3(Vitals/Skills/CommonButton)styled/compiled/saved; parentMain+Switcherdirtyfromdefaultpropagation guardedbeforewrite, remaining20+structuralheaders/linearvitalsnotdone. NoPIEstarted.

LATEST EDITOR CHECKPOINT: user reopened;18reflectedpresentationfields+QuestCategory verified. Module0 full45owned-widget baseline complete, actual11×7/TileSize50 retained. Module1 18sharedresourcescreated/configured/nativecompiled/verified/SAVED:2boldfontassets+14CommonTextStyles+2CommonButtonStyles. Production8PNGs/nativeXCFs complete; rootvisualQA passed. MapPilotpreflight backups+calibration verified. FirstStorytexture importcompletedbut immediateasyncrebuild sizecheckfailed; fresh128×128 readandexactPNGsourceconfirmed. Importer recoveryinprogress; widgetstyleassignmentsNOTyetapplied. Receipts Saved/FieldNotesImplementation/{reflection-after-reopen.json,Baseline/baseline/manifest.json,Resources/resources-readback.json,MapPilot/preflight.json}. Rootonlyeditorwriter; helperrecipesfile-only. UserstoppedPIE; rootstoprequestobservedIdle, noPIEstarted. ContinuefullapprovedFieldNotesimplementation; do notclaimresourcesalonefinishit.

LATEST: reviewed8-file nativeUIpatch applied and normal AZEditor build PASSED13actions/14.31s; DLLlinked. User explicitly: **do not launch Unreal Editor; he will open it himself. Only build and report completion at this checkpoint.** Root didnotlaunch. New reflectedfields mustbeverified afteruserreopens beforeassetassignment. Receipts Saved/FieldNotesImplementation/module3-build-receipt.json, module3-UBT-succeeded.log, source-applied.json. FieldNotesruntimeassetstyling stillpending; token/resourceauthoringrecipeprepared, productionartindependentlyprepared. Do notcallfullUIimplementationcomplete.

Artur selected **02 Field Notes / Полевые записи** and explicitly authorized full implementation as drawn. He then clarified the required order: **carefully audit and write a detailed module implementation plan FIRST, then proceed according to it**. He also requested inspecting `/Game/MenuSystemPro` for useful existing menu functionality/optimization. This supersedes the older selection-pending text below. Current pass begins with read-only audit and planning; no permission to start PIE/tests is implied. Root owns editor mutations and the main plan; helpers currently audit native style integration, MenuSystemPro reuse, and the exact art/style contract. Preserve current functionality while applying the common style; verify pack runtime dependencies before integrating any new screens.

September19 user explicitly requested THREE different coherent mockup families for all menus, including quick select. Existing quick menu is generally good; unify indicators rather than replacing its established structure. This resumes the previously deferred whole-game design task, but authorizes mockups/review only, not changing gameplay assets.

Deliverables: `C:/UnrealEngine/Games/AZ/UI Design/CHALK_UnifiedUI_v01/`. Families01QuietSignals(Тихие сигналы),02FieldNotes(Полевые записи),03DistrictSigns(Городские ориентиры). Each has12 full1920×1080 PNG studies plus one layered `CHALK_AllMenus_NATIVE.xcf`; three overview PNG/nativeXCF sheets. Current creator: `Tools/unified_ui_mockups_gimp.py`, shared indicator sheet `Tools/unified_ui_indicator_sheet.py`. Per-theme manifest records native Text/Vector/Group counts and reopened-master readback. `README.md` links the sources. `Review.html` is a local convenience gallery; automatic browser verification was blocked by browser URL policy, so deliver PNG/XCF previews directly rather than claiming browser verification.

Screens: Inventory, Map/Journal, Quick Select, HUD, Pause, Settings, Title, Save/Load, Item Actions, Confirmation, Loading, Indicators. Existing MenuSystemPro Silence menu assets supplied the ancillary-screen scope; their actual CHALK runtime wiring was not verified. No recipe/workbench feature was invented for Craftables, which remains a category tab. Example content/values and drawn district geometry are illustrative.

Preserve: inventory3columns (portrait/vitals;backpack/currency/categories/grid/details;skills/details). Portrait exported from actual inventoryHeroPortraitImage `/Game/AZ/Assets/UI/Textures/T_UI_Hero_Cut_V2_NoBackground_Bloor`; it is existing placeholderart, not a newly selected protagonist. QuickV5 exact8cells0–7,104×72,center1368/468 at1920×1080,12px gaps,4arrows;mode0 is DESTINATION,HUDmode is CURRENT. No controls/runtime changes.

Shared proposed semantics in all3: StorydoubleDiamond/warmaccent;Sidecircle/coolslate;PersonalopenCorners/sage;focusneutralframe;equippedsteadylowernotch;assignmentborder-onlypulse;completedcheck;failedcross/red;optionaltextmodifier. Different font families per quest type are deliberately avoided.01 usesOswaldLight/Roboto overcharcoal;02Roboto overpaper withdarkbackedHUD;03OswaldMedium/Roboto andfirmrails/opaqueblue-sootpanels. Selection pending; do not treat any palette/iconmapping as approved implementation.

QA corrected Equippables screenshot to equipment-only conten