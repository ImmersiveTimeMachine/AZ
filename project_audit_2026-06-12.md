---
name: project_audit_2026-06-12
description: "Full 5-agent design+correctness audit of the v2 character system (anim C++, pawn/Mover, GAS, data/tooling, hygiene). Verdicts on every major design decision (all sound; one structural debt: AnimInstance orchestrates sim-affecting RM moves). Prioritized bug list with file:line — 3 findings spot-validated by direct read. THE backlog for cleanup/MP-hardening work."
metadata: 
  node_type: memory
  type: project
  originSessionId: 787f844b-69e1-48c0-8b39-9a9264829d57
---

# v2 System Audit — 2026-06-12 (5 parallel auditors + spot-validation)

## ★ EXECUTION RECORD (same day) — P0 + scalability pre-work + P1 + P3 IMPLEMENTED, **BUILD SUCCEEDED** (editor-closed CLI, `Result: Succeeded`, UnrealEditor-AZ.dll linked; PIE-verify pending)
**5.8 API gotcha hit during the build:** `UAbilitySystemComponent::Add/RemoveReplicatedLooseGameplayTag` DO NOT EXIST in 5.8 — the replicated loose-tag surface is `GetReplicatedLooseTags_Mutable()` (returns `FMinimalReplicationTagCountMap`, `.AddTag/.RemoveTag`) and it is **protected** → must be called from an ASC SUBCLASS member (the AZ helpers are now `UAZ_AbilitySystemComponent::AddStateTag/RemoveStateTag`, protected members: local loose tag always + map entry on authority). The compile-time `bSkipInterpolation` patch probe COMPILED → engine patch verified present. Pre-existing deprecation warnings in AZ_Weapon.cpp (NetUpdateFrequency public access :20, FindObject bool ExactClass :262) — not from this pass, fix before next engine upgrade.
All four phases executed 2026-06-12 (one editor-closed build). What landed (✅) vs deliberately deferred (⏳):
- ✅ P0 1-7 ALL: item crash guards; LastPushed* commit moved post-push + NEW `TransitionSerial`/`LastPushedTransitionSerial` lock (replaces SMState-equality); camera reads `CharacterMover->IsCrouching()` (new cached ref, GAS-tag fallback for legacy pawn); `Source/AZ/Generated/` deleted + gitignored; dead `FAZ_LocoSMInputs::PreviousStance` removed; v1 overlay gated behind NEW `bShowAnimDebug` + inventory prints stripped + `bDebugTrajectory=false` + [v2 TRANS] log → Verbose; mangled comment rewritten. Plus P2 one-liners: idle-break Remaining=Len−StartTime; init resets (PendingBlendOut/LastPushed*/serials/RM-flag) in NativeInitializeAnimation.
- ✅ Pre-work: `FAZ_LocoSMInputs.MovementMode` (enum, replaces bInAirMode) + `bSuppressLocomotion` (SM pins IdleLoop, consumes stance flips; AnimInstance UPROPERTY hook); `FAZ_v2_ChooserContext.FromStance` (= SM `GetSettledStance()` — NEW accessor; old 3 dead timer accessors deleted); typed RMAction-kind = AnimInstance reads the mode's `bHandOffToFallingAtApex` via `FindMovementModeByName` (bHoldTakeoffPhase no longer name-sniffed — vault-safe).
- ✅ P1: GA_PawnJump release via `UAbilityTask_WaitInputRelease` (bTestAlreadyReleased, mirrors GA_Crouch; raw InputReleased override REMOVED — was the server-never-ends bug); tap-drop fix (release no longer clears the one-shot; ProduceInput sole consumer; no-PC path now consumes + zeroes custom inputs); RMAction detector reset via engine `Activate()` lifecycle override (sim-time-gap heuristic + ExpectedNextTickStartMs DELETED); RM-cancel broadened to ANY RMAction-exit edge; Walking `bJustLanded` = SIM-TIME (`Activate` records `LandedSimTimeMs` from `FMoverEventContext.EventTimeMs`; `GenerateMove_Implementation` override derives the window; delegate+FTimerManager machinery DELETED, bJustLanded now `mutable` plain member); compile-time patch probe (`AZ_Probe_MoverSkipInterpolationPatch` touches `bSkipInterpolation` in AZ_PawnMoverComponent.cpp — engine sync that drops the patch = compile error); loose tags authority-aware (`Add/RemoveReplicatedLooseGameplayTag` on authority, local elsewise — static helpers in AZ_AbilitySystemComponent.cpp); PC `GetAbilitySystemComponent` resolves PlayerState FIRST (pawn-interface fallback kept) + input logs Warning→Verbose; GA_Crouch avatar gate (CharacterMoverComponent || ACharacter); ProxyMesh resolves owner chain (Owner-controller → Owner-pawn → Instigator → FirstPC fallback).
- ✅ P3: notify writers register on track 0 + `RefreshCacheData()` (helper `RegisterNotifyOnTrackAndRefresh`, all 5 adders + RemoveAll — the dangling-pointer crash fix); SetCellEnumOnSub honest-fails BEFORE writing Comparison; SetChooserRowMultiEnumValues bitmask by INDEX + all-or-nothing; CompileAndSave returns the real save result; 3 column-add backfills now MATCH-ANY; AddOutputStructColumnToSub resolves ContextIndex by scanning Root->ContextData for the matching struct type (hard default removed, honest fail if absent); AddAnimRow hard-guarded to the exact legacy 3-enum-column layout; `rtg_rm_`/`rtg_` added to the fuzzy-matcher prefix table; `Cahed_CharacterMoverComponent` → `Cached_CharacterMoverComponent` (code + both redirect targets + old→new redirects); ini: dup ItemManifestData line + 4 self-redirects removed.
- ⏳ DEFERRED (with reasons): RMAction 4-field sync-state migration (lifecycle reset landed; full FMoverDataStructBase block stays behind its documented trigger "if MP resim jitter shows" — not PIE-testable today); pivot hysteresis P2-17 + crouch-mid-air P2-16 (FEEL/design changes — need PIE decisions); OnUnPossess CancelAbilities (PlayerState-resolve already fixes the stuck-tag path; tag-targeting needs GA AbilityTags audit); legacy v1 GA_Jump same-defect fix (deprecated path); v1 combat GA port (GA_Aim/Shoot dead on v2 pawn — feature work); SmoothWalkingMode-patch retirement by vendoring (separate task); EventData parameterization doctrine (design work); LM_RM_ batch quarantine (content op); ~50 inventory-era redirect retirement (needs content resave pass).
- PIE-verify checklist after build: walk/run/crouch loops + starts/stops/turns; jump (idle/walk/run, repeated taps, fast tap); crouch transitions (incl. crouch pressed mid-stop); land rows; camera crouch framing under a low ceiling; BlendOut rows 39/40/52/53 (field is now LIVE — first build with it); no on-screen debug spam.

Method: 5 agents (anim C++ / pawn+Mover / GAS+input / data+tooling / hygiene grep sweep) over the CURRENT code, cross-checked against memory. The 3 highest-stakes claims verified by direct Read: ✅ jump-GA server-end bug, ✅ LastPushed* early-commit wedge, ✅ AZ_Item null-deref crash. Asset-side claims verified against the live CHT dump + disk imports.

## DESIGN-DECISION SCORECARD (the "did we choose wrong?" answer: NO — every pillar held up)

| Decision | Verdict | Caveat that matters |
|---|---|---|
| **Mover over CMC** | ✅ SOUND, paying off | InputCmd-pure gait/crouch bridge, `ResolveRotationTarget` AI seam, RMAction mode = impossible-in-CMC wins. Patch burden = 2 behavioral patches; needs LOUD-FAILURE containment (see P2-9). |
| **Pure C++ SM (not AnimGraph SM)** | ✅ SOUND | Deterministic-by-construction EXCEPT idle-break `FMath::FRandRange` (proxies diverge — cosmetic, but invalidates the blanket claim; seed by NetGUID or annotate). |
| **Chooser + BlendStack (flat 54-row CHT)** | ✅ SOUND for now | Flat breaks at the direction/sprint expansion (combinatorial rows + O(rows) scripted-edit re-verification). Split per-SMState into nested sub-choosers BEFORE the strafe expansion, not after. |
| **Hybrid jump (RM rise → physics fall → MM land)** | ✅ SOUND | Z-delta apex detector is right-for-now; end state = per-clip AUTHORED apex time in the chooser row, enforced in sim (kills the detector's epsilon/teleport edge cases wholesale). |
| **TransitionStance (generic, not per-target)** | ✅ SOUND | Pre-Prone requirement: surface a `FromStance` field in outputs+context (with 2 stances FROM is implied; Crouch2Prone vs Stand2Prone is undecidable without it) + handle stance re-flip DURING TransitionStance. |
| **GAS-as-bookkeeping** | ✅ executed in the v2 spine | Doctrine FRAYS at the edges: camera reads intent tags; v1 combat GAs write AnimInstance bools directly (dead on v2 pawn); "EventData picks variant" is implemented NOWHERE (aspirational doctrine). |
| **BlendOut sentinel (<=0 = no override)** | ✅ OK | Conflates "no override" with "instant cut" — document `-1=none, 0=hard cut` or add a bool if a 0s release is ever needed. |
| **⚠ THE one structural debt** | AnimInstance queues/cancels sim-affecting layered moves (`AZ_MoverAnimInstance.cpp:104-107,:186`) | A montage-era habit transplanted into Mover: a render-coupled game-thread object originates sim moves. Works in SP; THE thing to redesign before co-op (move the seam into sim — mode invalidates via blackboard on the tick it sets NextModeName). |

## P0 — REAL BUGS, CHEAP, FIX NOW

1. **✅VALIDATED CRASH `AZ_Item.cpp:163-164` + `:198-199`** — `if (const auto Hero = Cast<AAZ_HeroCharacter>(OtherActor); const auto C = Hero->GetEquipmentManagerComponent())` derefs `Hero` BEFORE any null test; any non-hero overlap (a Chalky!) = null-deref crash. EndOverlap lacks even the `!OtherActor` check. 2-line fix.
2. **✅VALIDATED transition-wedge `AZ_MoverAnimInstance.cpp:434-438`** — `LastPushed*` committed BEFORE the push validates; abort paths at `:497` (MM null + no fallback) and `:504` (MMCostLimit reject) leave the cache poisoned → the transition lock at `:411` suppresses every retry for the whole transition (clip never plays, 1.0s fallback timer saves the SM). Latent today (all rows MMCostLimit=0) but armed the day a cost limit is set. Fix: move the commit next to the PendingBlendOut refresh at `:538`.
3. **Lock-key staleness `AZ_MoverAnimInstance.cpp:411`** — lock compares raw SMState; a bailed push leaves a stale `LastPushedSMState` that can suppress a LATER same-state transition (stop → bailed jump push → land-to-stop locked out). Fix: per-entry transition serial (increment on SMState change in NativeUpdateAnimation), compare that.
4. **Camera reads GAS intent `AZ_PawnCameraMovementComponent.cpp:112-114`** — crouch/aim/sprint framing off ASC tags; capsule reads Mover result. Concrete failure: ceiling-blocked uncrouch = capsule stays crouched, camera pops to standing. Point at `CharacterMover->IsCrouching()`.
5. **`Source\AZ\Generated\UnrealClaude\Live_Coding_*.cpp`** — MCP Live-Coding scratch TU COMPILES INTO THE MODULE (UBT globs all .cpp). Delete + add `Source/AZ/Generated/` to .gitignore.
6. **Dead twin `AZ_LocomotionStateMachine.h:35`** — `FAZ_LocoSMInputs::PreviousStance` never written/read (the live one is the SM's private member h:116). Delete; it invites someone to set it and expect an effect.
7. **TEMP DEBUG overlay `AZ_AnimInstance.cpp:985-1131`** (v1, self-labeled "remove after testing", ~15 ungated per-frame screen prints) + inventory debug prints (`AZ_EquipmentManagerComponent.cpp:222-224`, `AZ_Inv_CommonUI_GameInventoryMenu.cpp:27/51/56/122`). Gate like `bDebugTrajectory` or delete. Also `AZ_MoverAnimInstance.h:111` `bDebugTrajectory = true` default → flip to false.

## P1 — BEFORE ANY MP/CO-OP MILESTONE (the "2-player listen-server PIE just works" bar)

8. **✅VALIDATED jump GA never ends on server** — `AZ_GA_PawnJump.cpp:59-66` ends only via the `InputReleased` virtual; the ASC invokes that only on the owning client (`AZ_AbilitySystemComponent.cpp:83-102`); LocalPredicted server instance stays Active forever → remote client jumps exactly ONCE. Same in legacy `AZ_GA_Jump.cpp:53-60`. Fix: `UAbilityTask_WaitInputRelease` (as GA_Crouch already does — its task replicates release), or End immediately and carry "held" via the pawn flag.
9. **Engine-patch containment** — `bSkipInterpolation` has ZERO compile-time coupling (referenced only in a comment, `AZ_PawnMoverComponent.cpp:91`); engine upgrade that drops it regresses SILENTLY (proxy pop returns) and mixed builds DESYNC (NetSerialize wire change). Add a startup probe that fails loudly + keep the .patch applied by script. `USmoothWalkingMode` export patch fails loud (linker) — retire by vendoring the spring-damper math into UAZ_PawnMovementMode_Walking.
10. **Sim-mutable state on mode instances** — RMAction `NetRiseCm/NonPositiveZDeltaTicks/RiseElapsedMs/ExpectedNextTickStartMs` (`AZ_PawnMovementMode_RMAction.h:86-89`, self-acknowledged) + Walking `CachedRotationOffsetDegrees` and wall-clock `bJustLanded` (FTimerManager, `AZ_PawnMovementMode_Walking.cpp:69-86`) — all break under resim. Migrate to an AZ `FMoverDataStructBase` sync block; derive bJustLanded from sim-time of the Falling→Walking edge.
11. **Sim-proxy anim inputs** — `GetLastInputCmd()` reads for bIsMoving/Gait (`AZ_MoverAnimInstance.cpp:146-151,:205-213`) empty on proxies without bSyncInputsForSimProxy — self-acknowledged "pending MP fix"; the single biggest doctrine gap.
12. **Loose tags don't replicate** — `OnWeaponEquipped`/`OnMovementStateChanged` use non-replicated Add/RemoveLooseGameplayTag + instance caches (`AZ_AbilitySystemComponent.cpp:116-164`); use AddReplicatedLooseGameplayTag or infinite GEs.
13. **Possession-gap input loss** — PC resolves ASC via `GetPawn()` (`AZ_PlayerController.cpp:45-52`); release during pawn swap = stuck crouch tag on next pawn. Resolve via PlayerState ASC + cancel Movement.* abilities in OnUnPossess. No death/unpossess `CancelAbilities` exists at all; GA_Crouch has NO avatar gate (`AZ_GA_Crouch.cpp:36-46` returns true unconditionally — copy GA_PawnJump's interface gate, `AZ_GA_PawnJump.cpp:26-29`).
14. **`AZ_Inv_ProxyMesh.cpp:42`** `GetFirstPlayerController()` — every proxy mesh mirrors player 0. Resolve via owner chain.
15. **Apex detector hardening** — float-equality fresh-activation check (`AZ_PawnMovementMode_RMAction.cpp:102`) can inherit stale state on exact-tick re-entry / spuriously reset on drift → use Activate/Deactivate lifecycle. Zero-Z-delta clip or mid-rise teleport = up to 1s hover (bound MaxRiseSeconds by actual clip length — AnimInstance knows `Remaining`). RM-cancel is render-frame-latent (`AZ_MoverAnimInstance.cpp:183-187`) and only fires on RMAction→Falling — cancel on ANY RMAction-exit edge (RMAction→Walking leaves a live OverrideAll move driving the capsule on the ground).

## P2 — CORRECTNESS EDGE CASES (SP-visible, fix as touched)

16. **Crouch mid-air is silently absorbed** — air branch settles PreviousStance every airborne frame (`AZ_LocomotionStateMachine.cpp:87`); on touchdown no TransitionStance fires AND land rows filter Stance=Crouching with no crouch-land rows in the CHT → chooser returns nothing, pose holds. Block crouch while airborne at GAS/Mover layer, or defer the stance settle to touchdown.
17. **Pivot ping-pong** — `|PendingStartAngleDeg| >= 135°` re-triggers TransitionToLocomotion with no hysteresis (`AZ_LocomotionStateMachine.cpp:158,:179-186`); circling stick = pivot loop. Add re-arm (angle < ~90° since last pivot) or min dwell.
18. **Jump tap < 1 frame dropped** — GA release path clears `bIsJumpJustPressed` (`AZ_PawnMoverHeroCharacter.cpp:298-301`) before ProduceInput consumes (`:421`). Let ProduceInput's consume be the sole clearer. Also: no jump buffering near landing; `CanRequestJump()` never overridden (default true) — the "GAS gates the jump" claim is currently vacuous.
19. **Idle-break end ignores StartTime** — `NotifyIdleBreakClipPushed(PlayLength)` (`AZ_MoverAnimInstance.cpp:553`) vs transition path's `PlayLength - StartTime` (`:576-577`).
20. **PendingBlendOut not reset in NativeInitializeAnimation** (`cpp:22-48`) — re-init/LiveCoding can inherit a stale BlendOut. Reset PendingBlendOut + LastPushed* + bPendingTransitionRMMove there.
21. **ChooserOutputs written on bail paths** (`AZ_MoverAnimInstance.cpp:382`) — ABP bindings see outputs of evaluations that never pushed; move after bail checks. Worker-thread store-order contract at `:604-605` (duration-before-flag) is load-bearing and uncommented — comment it; consider atomic release/acquire.
22. **bHoldTakeoffPhase conflates RMAction with jump-rise** (`AZ_MoverAnimInstance.cpp:301` + mode-name string sniffing `:156-171`) — the FIRST VAULT will be classified as a jump takeoff and held in TransitionToInAir forever (vault never hands to Falling). Traversal needs a typed "RMAction kind" signal (tag on mode / field on component), not name sniffing. FLAGGED IN THE TRAVERSAL PLAN.
23. **NativeUpdateAnimation god-tick** (~320 lines, ≥4 silent ordering contracts: RM-queue before teardown; PendingStartAngle before SM tick; air latch after SM tick — SM and chooser INTENTIONALLY see different bIsMoving in air, stated nowhere; PreviousSMState snapshot before tick). Extract named steps with the contract documented at the call site.
24. **GA edge rot**: GA_Aim toggles `bIsAiming` every Triggered tick via Held-retrigger (`AZ_AbilitySystemComponent.cpp:67-81` + `AZ_GA_Aim.cpp:36-37`); `Instances.Last()` without Num() check (`:58-61,:95-98` — asserts on NonInstanced); GA_Interact mutates inventory via PC method on a LocalPredicted ability (double-add risk, `AZ_GA_Interact.cpp:23-30`) and uses a 2s active-timer instead of a Cooldown GE; GA_Shoot raw `SetNumericAttributeBase` ammo (`AZ_GA_Shoot.cpp:330-340` — use an instant GE); v1 GA_Aim/Shoot write AnimInstance bools directly and are DEAD on the v2 pawn (no tag granted, nothing replicates — the v2 port should grant Ability.State.* and let anim read tags).

## P3 — TOOLING HARDENING (AZ_*Utils silent-success paths; ~⅓ of Chooser mutators can return true after doing nothing)

25. **Notify writers bypass the editor track cache** — all six Add*Notify in `AZ_PoseSearchUtils.cpp` (81-92, 160-171, 183-194, 208-219, 233-244) never touch `TrackIndex`/`RefreshCacheData()` → notifies invisible until PostLoad (the 2026-06-12 "I don't see notifies" saga). WORSE: `RemoveAllPoseSearchNotifies` (`:248-270`) RemoveAt's without RefreshCacheData → DANGLING raw FAnimNotifyEvent* in AnimNotifyTracks[].Notifies = latent editor crash. Fix in each: `#if WITH_EDITOR if (Sequence->AnimNotifyTracks.Num()==0) Sequence->InitializeNotifyTrack(); Evt.TrackIndex=0; Sequence->RefreshCacheData(); #endif`. (Python-side workaround already in [[reference_ue5_python_anim_notifies]].)
26. **`SetCellEnumOnSub` returns true on no-op** (`AZ_ChooserUtils.cpp:1584-1606`) AND still writes Comparison on failure (can flip Any→"Equal stale"). `AddAnimRow` (`:124-175`) assumes 3 columns, appends cells only to c0-2 → MISALIGNS every later row's cells on the 11-column CHT, check()s on non-enum columns, silently discards its bUseMM/BlendTime args. `SetChooserRowMultiEnumValues` builds bitmask by raw VALUE (`:592`) while `SetCellMultiEnumOnSub` correctly uses INDEX (`:1635`) — dormant until a sparse enum. `CompileAndSave` drops the save result (`:224`). Column-add backfills are CONSTRAINING not Any (`:1245,:1278,:1302` — adding an enum column to a populated table turns every row into "must equal value 0"). `AddOutputStructColumnToSub` still defaults ContextIndex=1 (the documented v2 footgun, `:1450-1455`).
27. **Two parallel anim batches = standing cross-match hazard** — `LM_RM_*` (Content\AZ\NoWeapons\RootMotions\, v1) and `RTG_RM_*` (Content\Assets\RM_Movement\, v2) duplicate ~100 names; `AutoRemapChooserAssets` strips `lm_rm_` but NOT `rtg_rm_` in its normalizer (`:1790-1795`) — fuzzy pass can silently swap a row to the other batch. Add rtg_rm_ to the prefix table; quarantine/exclude the LM batch from SearchPaths. (The 2026-06-12 scare "DBs index a different batch than the chooser plays" was an inspection mislabel — disk-verified: PSD_v2_Jump = 5 RTG lands, PSD_v2_Loco_Loops = 2 RTG loops, CHT 100% RTG. But the hazard that made it plausible is real.)
28. **`Cahed_CharacterMoverComponent` is the REAL C++ name** (`AZ_MoverAnimInstance.h:138`) — the DefaultEngine.ini redirects (:168,:171) faithfully preserve the misspelling. Rename property + retarget redirects + add old→new redirect. Also: 4 self-redirects (:143-146), 1 exact duplicate (:117/118), ~50 inventory-era redirects retireable after a content resave. `UnrealClaude.SkipScriptPermissionDialog=1` lives in shipped DefaultEngine.ini [ConsoleVariables] — move to editor-only config.
29. **Dead/rotted**: physics-jump branch content-rotted (chooser rows authored for hybrid ST=0; flipping bUseHybridJump=false today desyncs takeoff — gate rows on the flag or delete branch + 3 JumpUpwardsSpeed* props). `FAZ_MoverCustomInputs` RotationOffset/MovementDirection/RotationMode/ControlRotationRate never written by v2 pawn (2 zero doubles serialized per cmd; `Interpolate` lerps RotationOffset across ±180 wrap; `Merge` drops gait — fix when packed). Camera component carries CMC-era config (7 dead props) + hard-casts v1 pawn (`AZ_PawnCameraMovementComponent.cpp:123` — move-offset silently no-ops on v2). LocomotionLoopDatabase doc-comment describes retired behavior (h:53-57). Timer accessors not UFUNCTION + uncalled (`AZ_LocomotionStateMachine.h:101-104`). GA_Jump ctor tag-remove no-op (CDO-time, `AZ_GA_Jump.cpp:12`). bCannotActivateWhileInteracting inert (tags never assigned). ActivationPolicy/ActivationGroup read by nothing; legacy InputID still live in weapon granting (`AZ_Weapon.cpp:275`) — two ability-input systems coexist. UAZ_GA_Unarmed_Jumping_AnimState empty shell + the 4-weapon×12-state Ability.Animation.* tag matrix (AZ_GameplayTags.cpp:314-363,389-434) feed the abandoned per-anim-state scheme — delete or actually adopt EventData.
30. **Mangled comment fossil** `AZ_MoverAnimInstance.cpp:377-381` (corrupted interleaved edit referencing a removed param). `const_cast<UBlendProfile*>` duplicated on both push paths (`:513,:531`) — hoist.

## SCALABILITY PRE-WORK (do BEFORE the feature, all additive)
- **Slide**: pass `EAZ_MovementMode` into FAZ_LocoSMInputs instead of a third bool (bInAirMode flag-pile).
- **Prone**: FromStance in outputs+context; stance re-flip during TransitionStance.
- **Vehicles**: `bSuppressLocomotion` SM input (pin IdleLoop, freeze timers — one early-return). Per-pawn ability grant sets (`bStartupAbilitiesGiven` is once-per-ASC-forever, `AZ_AbilitySystemComponent.cpp:16-19` — vehicle StartupAbilities will be silently skipped).
- **Traversal**: typed RMAction-kind signal (P2-22).
- **Direction/sprint expansion**: nested per-SMState sub-choosers FIRST. Order-sensitive today: i4/i8 loop rows; stop rows must pin c8 bJustLanded=False (not Any) or they shadow the land rows i35-38; rows 52/53 must stay the only TransitionStance rows.

## HYGIENE (validated counts)
CVar lifetime rule: FULLY COMPLIANT (4 CVars, module-lifecycle, unregister by name). 11 TODOs cataloged (foley wiring, GATA AI muzzle fallback, tag-query refactors). Magic numbers top-10 listed in audit (takeoff 0.20, ReversalAngle 135, apex `NetRiseCm>10 && ticks>=2`, TIP 60°/0.998, 7× `Now+1.0f` placeholders, deadzone 0.1, contact 0.5, trajectory 0.04/10/0.1/10) — jump impulses 350/500/550 already exemplary UPROPERTYs. MEMORY.md "Key Source Directories" map is stale (missing AbilitySystem\, Data\, Game\, Input\, Interaction\).
